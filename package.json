{
  "name": "truthlayer-ai-workspace",
  "version": "1.0.0",
  "description": "Root workspace orchestrator for TruthLayer AI fact checker",
  "scripts": {
    "dev": "start cmd /k \"npm run dev-backend\" && npm run dev-frontend",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps"
  }
}
